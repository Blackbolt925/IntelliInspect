server {
  listen 80;
  server_name localhost;

  # 🔥 ADD THIS LINE to allow large file uploads (e.g., up to 500MB)
  client_max_body_size 500M;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
